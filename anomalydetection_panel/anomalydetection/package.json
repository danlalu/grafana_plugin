{
  "name": "Anomaly detection",
  "version": "1.0.0",
  "description": "Anomaly detection powered by HoursAI",
  "scripts": {
    "build": "grafana-toolkit plugin:build",
    "test": "grafana-toolkit plugin:test",
    "dev": "grafana-toolkit plugin:dev",
    "watch": "grafana-toolkit plugin:dev --watch",
    "sign": "grafana-toolkit plugin:sign",
    "start": "yarn watch"
  },
  "author": "Dean.zhang",
  "license": "Apache-2.0",
  "devDependencies": {
    "@grafana/toolkit": "latest"
  },
  "dependencies": {
    "@grafana/data": "latest",
    "@grafana/runtime": "^9.3.2",
    "@grafana/ui": "latest",
    "echarts": "^5.4.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "engines": {
    "node": ">=14"
  }
}
